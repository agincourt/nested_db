%h1= "Delete #{@taxonomy.name.titleize}"

= form_for([@taxonomy, @instance], :url => url_for(:action => :destroy, :taxonomy_id => @taxonomy.id, :id => @instance.id), :html => { :method => :delete }) do |f|
  
  %p= "Are you sure you want to delete this #{@taxonomy.name}?"
  
  %p
    = link_to '« No', taxonomy_relative_to_instance_path
    %button{ :type => 'submit' } Yes »